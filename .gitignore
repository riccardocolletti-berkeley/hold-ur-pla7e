# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.eggs/
.pytest_cache/
.coverage
htmlcov/

# Virtualenvs
.venv/
venv/
env/

# Tooling caches
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# uv
# (uv.lock IS committed; uv-managed venvs and caches are not)

# Editor / IDE
.vscode/
.idea/
*.swp
*~
.DS_Store

# Per-machine scratch
local/

# Runtime / generated artefacts
# Inline `#` is part of the pattern in .gitignore, so comments stay on
# their own lines (otherwise nothing under sim/policies/ etc. is ignored).
# RL checkpoints + tensorboard
sim/policies/
# eval CSVs / plots
sim/results/
# drawn JSON saved at runtime
designer/trajectories/
# drawings staged for the real robot
real/ros2_ws/src/ball_balance_controller/runtime/

# Colcon scratch (regenerated by `colcon build`)
**/install/
**/build/
**/log/

# Third-party deps cloned at runtime
sim/external/mujoco_menagerie/

website_old_skeleton_backup/
